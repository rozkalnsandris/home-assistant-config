# Intentionally contains no real values.
#
# After the read-only live inventory, add only secret key names with clearly
# non-secret placeholder values that preserve the expected value type.
# Real secrets.yaml remains outside Git.
